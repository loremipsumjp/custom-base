extends ./screen

block header
  ul.nav.navbar-nav.navbar-right
    li.dropdoxn
      a.dropdown-toggle(href='#', data-toggle='dropdown') #{user.name}さん <span class="caret"></span>
      ul.dropdown-menu
        li
          a(href='/private/auth/password/') パスワードの変更
        li.divider
        li
          a(href='/private/auth/logout/') ログアウト

block content
  .row
    .col-xs-9
      .main
        block main
    .col-xs-3
      .aside
        ul.nav
          each item in profile.layout.aside.nav.items
            li
              a(href='#{item.url}') #{item.text}
